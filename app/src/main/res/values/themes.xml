<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SudokuMaster" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SudokuMaster" parent="Base.Theme.SudokuMaster" />
    <style name="AnimationSetting">
        <item name="android:windowEnterAnimation">@anim/toup</item>
        <item name="android:windowExitAnimation">@anim/todown</item>
    </style>
    <style name="AnimationOption">
        <item name="android:windowEnterAnimation">@anim/toup</item>
        <item name="android:windowExitAnimation">@anim/todown</item>
    </style>
    <style name="AnimationWinning">
        <item name="android:windowExitAnimation">@anim/toupwinning</item>
        <item name="android:windowEnterAnimation">@anim/todownwinning</item>
    </style>
</resources>